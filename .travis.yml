language: node_js

node_js:
  - "8"

services:
  - postgresql

install:
  - npm install

before_script:
  - psql -c 'create database more_recipes;' -U postgres
  - ./node_modules/.bin/sequelize db:migrate

script:
  - npm test

after_success: npm run coverage